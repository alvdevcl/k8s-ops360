apiVersion: v1
kind: Service
metadata:
  labels:
    app: dp-propagation-service
  name: dp-propagation-service
  namespace: uat01
spec:
  ports:
    - name: http
      port: 9000
  selector:
    app: dp-propagation-service
