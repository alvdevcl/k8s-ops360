apiVersion: v1
kind: Service
metadata:
  labels:
    app: alpha-publisher-derived-service
  name: alpha-publisher-derived-service
  namespace: uat01
spec:
  ports:
    - name: http
      port: 8087
  selector:
    app: alpha-publisher-derived

